MONGODB_URI=mongodb://127.0.0.1:27017/resolvehub
PORT=4000

# Optionally set VITE API URL for the frontend development:
# VITE_API_URL=http://localhost:4000

# Force using file-backed storage (no attempts to connect to MongoDB)
# FORCE_FILE_STORAGE=true
